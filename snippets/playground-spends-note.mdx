<Warning>
  A playground run is a real request. It sends as one of your workspace keys,
  it spends credit at the same rate an API call does, and it lands in Activity
  on that key's line. Nothing about it is a sandbox.
</Warning>
